# .github/workflows/auto_update_iptv.yml
name: Auto Update IPTV List

on:
  schedule:
    # 每天UTC时间2点运行（北京时间10点）
    - cron: '0 8 * * *'
  workflow_dispatch: # 允许手动触发
  push:
    paths:
      - 'process_iptv.py'

jobs:
  update-iptv:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出仓库
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 2. 设置Python环境
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      # 3. 安装依赖（包括playwright和requests）
      - name: Install dependencies
        run: |
          pip install requests playwright
          # 只安装chromium，不安装firefox和webkit以节省时间和空间
          playwright install chromium --with-deps
          echo "✅ 依赖安装完成"
      
      # 4. 运行处理脚本
      - name: Run IPTV processing script
        run: |
          python process_iptv.py
          echo "生成的CN.m3u内容预览："
          head -30 CN.m3u
          echo ""
          echo "生成的M3U链接文件预览："
          head -10 available_m3u_urls.txt
      
      # 5. 检查文件是否有变化
      - name: Check for changes
        id: changes
        run: |
          git status
          changed_files=$(git status --porcelain | grep -E "CN\.m3u|available_m3u_urls\.txt" | wc -l)
          if [ "$changed_files" -gt 0 ]; then
            echo "检测到IPTV文件有更新"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "没有检测到变化"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
      
      # 6. 提交并推送更改（仅在文件有变化时）
      - name: Commit and push changes
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add CN.m3u available_m3u_urls.txt
          git commit -m "🔄 自动更新IPTV列表 [$(date +'%Y-%m-%d %H:%M:%S')]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # 7. 没有变化时的通知
      - name: No changes notification
        if: steps.changes.outputs.has_changes == 'false'
        run: echo "✅ IPTV文件无变化，跳过提交。"
